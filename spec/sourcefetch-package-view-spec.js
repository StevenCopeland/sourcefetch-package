'use babel';

import SourcefetchPackageView from '../lib/sourcefetch-package-view';

describe('SourcefetchPackageView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
